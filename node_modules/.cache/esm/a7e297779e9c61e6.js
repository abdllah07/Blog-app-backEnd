let verify,User;_9e9‍.x([["authGuard",()=>authGuard],["adminGuard",()=>adminGuard]]);_9e9‍.w("jsonwebtoken",[["verify",["verify"],function(v){verify=v}]]);_9e9‍.w("../models/User",[["default",["User"],function(v){User=v}]]);


       const authGuard = async (req, res, next) => {
    if(req.headers.authorization && req.headers.authorization.startsWith("Bearer")) {
        try {

            const token = req.headers.authorization.split(" ")[1];
            const {id} = verify(token , process.env.JWT_SECRET);
            req.user = await User.findById(id).select("-password"); // -password now select the password 
            next()
            
        } catch (error) {
            let err = new Error("Not authorized , Token Failed" );
            err.status = 401;next(err);
        }
    }else {
        let error = new Error("Not authorized , No Token");
        error.statusCode = 401;
        next(error);
    }
}




       const  adminGuard = (req , res , next) => {
    if(req.user && req.user.admin) {
        next()
    }else {
        let error = new Error("Not authorized , You are not an admin");
        error.statusCode = 403;
        next(error);
    }
}